import React from 'react'

const View = (props) => {
  console.log(props.Data);
  return (
    <div>    
        {/* <h1>{props.props.Title}</h1> */}
        {/* <h2>{props.props.Description}</h2> */}
    </div>
    )
}

export default View